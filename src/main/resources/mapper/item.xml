<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flabedu.small.small.repository.ItemMapper">

    <select id="list" resultType="com.flabedu.small.small.data.Item">
        select * from item;
    </select>

    <select id="getItem"
            parameterType="java.lang.Long"
            resultType="com.flabedu.small.small.data.Item">
        SELECT * FROM item WHERE item_id = #{id}
    </select>


</mapper>
